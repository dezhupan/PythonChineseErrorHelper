# 安装 Python 中文报错助手

你不需要解压安装包。

1. 打开 PyCharm。
2. 点击左上角菜单“文件”，选择“设置”。
3. 在设置窗口左侧点击“插件”。
4. 点击插件页面右上角的齿轮按钮。
5. 选择“从磁盘安装插件”。
6. 选择 `dist` 文件夹里的 `PythonChineseErrorHelper-2.1.0.zip`。
7. 点击“确定”或“安装”，然后按提示重启 PyCharm。

## 怎么使用

安装后照常点击 PyCharm 的运行按钮：

- 程序正常结束：插件不会弹出任何内容。
- Python 出现 traceback 并异常退出：“运行”窗口会自动转换成中文解释。
- 点击蓝色的文件和行号，可直接打开最可能需要修改的代码行；目标表达式会标黄，并弹出具体修改提示。
- 如果报错行只是使用了前面赋错的变量，插件会在证据明确时向上回溯到真正的赋值行；例如把 `self.word_count = corpus_idx` 标出并建议改为 `len(corpus_idx)`。
- 控制台会区分“已向上回溯的根因位置”和“错误触发位置（根因可能在前面）”。
- 出现明确替换建议时，可点击“确认后应用这条修改”；插件会先显示替换内容，确认后才修改，并可按 `Ctrl+Z` 撤销。
- “分析范围”会显示“控制台运行信息 + 当前报错文件完整扫描”；这里只读取当前报错 `.py` 文件，不会扫描整个项目。
- 点击“查看原始报错”，可恢复完整 traceback；点击“查看中文解释”可一键切换回来。
- 编辑 PyTorch 优化器代码时，如果把 `model.parameters` 写成了方法本身，编辑器会实时显示中文警告；按 `Alt+Enter` 可一键补成 `model.parameters()`。

## 卸载

进入“文件 → 设置 → 插件 → 已安装”，找到“Python 中文报错助手”，点击卸载并重启 PyCharm。
